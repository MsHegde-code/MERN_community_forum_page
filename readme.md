Pre-requisites
1. Install the required packages by executing -  npm install
2. You may need to install external packages by running this command: npm install lucid-react recharts jwt bcrypt

To run the application
1. In the client folder execute the command: npm run dev
2. In the server/src folder execute the command: node server.js
